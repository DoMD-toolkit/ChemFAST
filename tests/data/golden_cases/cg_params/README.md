# SPE CG parameter fixture

Compact deterministic SPE chemistry used to generate real `cg_parameters.json` and
`cg_system.xml` reproducibility goldens. It contains the A/B/C/L/S/T bead types,
A--A / B--A / C--A chemistry, and a connected component that exercises bond, angle and
dihedral parameter construction.
